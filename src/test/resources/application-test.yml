spring:
  data:
    mongodb:
      host: "localhost"
      port: 27017
      database: "spring_mongodb_test"

app:
  mongodb:
    indexes:
      - collection-name: "students"
        keys:
          roll: 1
        index-options:
          unique: "true"